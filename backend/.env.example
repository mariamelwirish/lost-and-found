# ==============================
# Django Configuration
# ==============================
DJANGO_SECRET_KEY=dev-please-change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# ==============================
# CORS / CSRF Configuration
# ==============================
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173,http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# ==============================
# Email (Debug only)
# ==============================
SENDGRID_API_KEY=replace-with-your-sendgrid-api-key
SENDER_EMAIL=aublostfound@outlook.com

# ==============================
# Database (Local Docker / Postgres)
# ==============================
DB_NAME=lf_db
DB_USER=lf_user
DB_PASSWORD=lf_password_123
DB_HOST=127.0.0.1
DB_PORT=5432

# Structured local database URL built from above info
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=

# ==============================
# Monitoring (Sentry)
# ==============================
SENTRY_DSN=https://f8a347a339ca450ebc565a7da9a9096e@o4510383110815744.ingest.de.sentry.io/4510383131918416
SENTRY_TRACES_SAMPLE_RATE=0.2
SENTRY_ENVIRONMENT=development
SENTRY_RELEASE=local
